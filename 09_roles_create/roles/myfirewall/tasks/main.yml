---
# tasks file for myfirewall
# fw_80, fw_443
- name: 패키지 설치
  ansible.builtin.dnf:
    name: firewalld
    state: present

- name: 서비스 기동 - firewalld
  ansible.builtin.systemd:
    service: firewalld
    state: started
    enabled: true

- name: 방화벽 등록 - {{ fw_80 }}, {{ fw_443 }}
  ansible.posix.firewalld:
    service: "{{ item }}"
    permanent: true
    immediate: true
    state: enabled
  loop: "{{ ports }}"