[defaults]
inventory = ./inventory      # 필수 설정
remote_user = ansible
ask_pass = false

[privilege_escalation]
become = true                # 필수 설정
become_method = sudo
become_user = root
become_ask_pass = false
